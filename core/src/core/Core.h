#pragma once

#include <cstdint>
#include <string>
#include <vector>

namespace pbe {

   using int64 = int64_t;
   using uint64 = uint64_t;
   using byte = unsigned char;

   using std::string;
   using std::string_view;

   using std::vector;

#define STRINGIFY(x) #x

}
